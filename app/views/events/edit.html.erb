<h1 class="center">Edit event</h1>
<%= render 'form', event: @event %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h3>Need to cancel?</h3>
    <p>No Stress. <%= button_to "Cancel my event", event_path(@event), class: "btn btn-danger", data: { confirm: "This can not be undone." }, method: :delete %></p>
  </div>
</div>
